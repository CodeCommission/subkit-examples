{
  "private": true,
  "version": "1.0.0",
  "name": "todo-app-api",
  "description": "Todo App GraphQL API",
  "engines": {
    "node": "^6"
  },
  "dependencies": {
    "uuid": "^3.1.0",
    "pouchdb": "^6.3.4",
    "pouchdb-adapter-memory": "^6.3.4",
    "subkit": "latest"
  },
  "scripts": {
    "dev": "subkit serve --port 8080 --graphiql",
    "dev:mocked": "subkit serve --port 8080 --mocked",
    "start": "NODE_ENV=production subkit serve --port 80 --graphiql",
    "start:secure": "NODE_ENV=production subkit serve --port 80 --secret SuperSecret --graphiql"
  }
}
